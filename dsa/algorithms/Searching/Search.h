#ifndef SEARCH_H // Used preprocessors directive to prevent double import in same file
#define SEARCH_H
// Header file for declarations

int binarySearch(int *arr, int target, int size);
int linearSearch(int *arr, int target, int size);

#endif
